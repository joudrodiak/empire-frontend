/**
 * Public import surface for the liquid-metal CTA.
 *
 *   import { LiquidMetalButton } from '@/components/ui/liquid-metal'
 *
 * Re-exports the canonical atom so call sites can use the conventional `ui/`
 * path. The implementation lives in atoms/LiquidMetalButton.
 */
export { LiquidMetalButton } from '@/components/atoms/LiquidMetalButton'
export type { MetalConfig } from '@/components/atoms/LiquidMetalButton'
